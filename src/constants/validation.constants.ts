/**
 * @module constants/validation
 * @description Constants related to form validation
 */

/**
 * Key used for root-level validation errors
 * (errors that don't belong to a specific field)
 */
export const ROOT_ERROR_KEY = '_root';

/**
 * Path separator for nested field errors
 */
export const FIELD_PATH_SEPARATOR = '.';
